# Arnol Works Web

Next.js frontend app for Arnol Works.

## Local development

```bash
npm install
npm run dev -- --hostname 0.0.0.0 --port 3000
```

Open `http://localhost:3000`.

Optional `.env.local`:

```text
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
```
